import Heading from '../../components/Heading';
import * as Styled from './styles';

const Home = () => {
  return (
    <Styled.Wrapper>
      <Heading>
        Olá
      </Heading>
    </Styled.Wrapper>
  );
};

export default Home;
